# 今日毕 · DoneToday 使用指南

> 今日毕（DoneToday）——一个零依赖、单文件、可离线使用的每日计划管理页面。双击即用，也可以部署到 NAS / 电脑实现手机、平板、电脑多设备同步。

---

## 目录

1. [它能做什么](#一它能做什么)
2. [三种使用方式](#二三三种使用方式)
3. [快速上手](#三快速上手)
4. [录入计划的五种姿势](#四录入计划的五种姿势)
5. [视图、筛选与排序](#五视图筛选与排序)
6. [自定义类别与分组](#六自定义类别与分组)
7. [多设备同步部署](#七多设备同步部署)
8. [导入与导出](#八导入与导出)
9. [键盘快捷键](#九键盘快捷键)
10. [家庭多人使用](#九家庭多人使用多用户)
11. [常见问题](#十一常见问题)

---

## 一、它能做什么

- **日历式管理**：默认展示一周计划，支持 日 / 周 / 本周总览 三种视图
- **四级优先级**：必须完成 > 尽量完成（默认）> 可以延后 > 无所谓，每天自动排序
- **生活 / 工作 / 行程 / 其他** 四个分区，一眼分清工作与生活
- **快速录入**：一行文字自动识别日期、时间、类别、优先级
- **组合模板**：如「基础健身 40min = 20min 有氧 + 15min 力量 + 5min 拉伸」，一键创建
- **逾期提醒**：过期未完成的任务自动高亮，并可一键筛选查看
- **跨设备同步**：通过 NAS WebDAV 或小工具实现手机 ⇄ 电脑实时一致，数据始终在你自己手里
- **批量导入导出**：CSV（Excel 可编辑）/ JSON（完整备份），可指定时间范围
- **零依赖**：无需安装、无需注册、无需联网，一个 HTML 文件走天下

---

## 二 / 三、三种使用方式 与 快速上手

| 方式 | 适合场景 | 数据存在哪 | 多设备同步 |
|---|---|---|---|
| ① 本地直接打开 | 单设备使用 | 浏览器 localStorage | ❌（可通过「远程同步」手动合并） |
| ② NAS WebDAV | 家庭 / 团队长期使用 | NAS 上的 `dayplan.json` | ✅ 实时 |
| ③ sync_server.py | 无 WebDAV 或想要快照回滚 | 电脑 / NAS 上的 `dayplan.json` | ✅ 实时 |

**最快开始**：双击 `index.html` → 点顶部输入框 → 输入标题回车，第一条计划就建好了。

> 建议长期使用方式 ② 或 ③（见[第七节](#七多设备同步部署)），所有设备打开同一份页面，数据天然一致。

---

## 四、录入计划的五种姿势

### 1. 快速输入框（最快）
页面顶部常驻输入框，输入后**回车即建**。支持快捷语法，输入时会**实时显示解析结果**：

```
!1 明天9点 拍摄 新品开箱视频
```
解析为：→ 明天 09:00 · 💼 工作·拍摄 · 必须完成

| 语法 | 示例 | 说明 |
|---|---|---|
| `!1` ~ `!4` | `!1` | 优先级：1=必须 2=尽量 3=延后 4=无所谓 |
| `!必须` `!延后` | `!必须` | 中文优先级，效果同上 |
| `今天` `明天` `后天` `大后天` | `明天…` | 指定日期 |
| `周一` ~ `周日` | `周五…` | 本周的某天（已过则自动顺延下周） |
| `9点` `9:30` `18点30` | `明天9点…` | 指定时间 |
| 类别关键词 | 拍摄 / 剪辑 / 脚本 / 有氧 / 力量 / 拉伸 / 组合 / 早餐 / 午餐 / 晚餐 / 备餐 / 买 / 出差 … | 自动识别类别与子项 |

什么语法都不写也可以，只输入标题，其余用默认值。

### 2. 类别名片的「两步录入」
以下入口都会打开同一个选择面板（类别名片 + 快捷模板）：

- 快速添加输入框**留空**时点「＋ 添加」
- 手机右下角悬浮 **＋**、电脑快捷键 `N`
- 周视图每个日期列底部的「＋ 添加」（自动定位到该列日期）

面板顶部会显示**目标日期**——点类别名片（已预填类别和子项）或点模板卡片，都创建到这一天。

### 3. 快捷模板（一键多条）
**应用入口（任选其一，均一键应用当天/选中日期）：**

- 快速添加区的「**📄 模板**」按钮（最直接）
- 「＋ 添加」打开的选择面板下半部分
- 周视图日期列的「＋ 添加」→ 面板中选模板
- 设置 → 模板管理 → 点模板名

**创建/维护模板：**
- 在编辑计划的弹窗里点「**存为模板**」，或到 设置 → 模板管理 新建
- 组合模板示例：`基础健身 40min` = 20min 有氧 + 15min 力量 + 5min 拉伸 → 点一下自动创建 3 条子任务，可分别勾选完成，卡片显示组合进度（如 2/3）
- 模板也可切换为「单条 + 内部清单」模式

### 4. 批量导入
点「导入」→ 粘贴文本（每行一条）或上传 CSV，先预览勾选再创建：

```
9-10, 拍摄, 上午拍新品视频, 必须完成
9-10, 健身, 下肢训练30分钟
明天, 行程, 去杭州出差
```
优先级可省略（默认「尽量完成」）；日期支持 `9-10`、`2026-09-10`、`9月10日`、`明天`、`周五` 等写法。

### 5. 复制已有的计划
- **单条复制**：点开任意计划 → 「复制到其他日期」→ 勾选多个日期批量复制
- **整日复制**：日视图 / 手机周视图的「整日复制」按钮，把一天的全部计划复制到多天
- **拖拽改期**（电脑）：周视图里直接把卡片拖到其他日期列

---

## 五、视图、筛选与排序

- **日 / 周 / 总览**：顶栏切换；总览把整周任务按分组 × 优先级平铺
- **筛选条**：全部 / 未完成 / 已完成 / **逾期**（跨所有日期列出过期未完成的任务）
- **自动排序**：每天的任务按 优先级 → 时间 → 创建时间 排列；「必须完成」标题加粗；已完成的划线沉底
- **逾期高亮**：日期已过且未完成的任务显示红边和「已过期」标签
- **诚实的完成机制**：勾选过期任务时会弹出三选一——「已完成」（正常划线）、「算是完成了吧」（绿色划线 + 标签）、「拖延症犯了」（自动顺延到今天并标记 😅）
- **进度**：每天列头显示「已完成/总数」

---

## 六、自定义类别与分组

打开 **设置 → 类别与分组管理**：

- **分组**（即周视图的分区列）：可改名、换颜色、新增 / 删除（分组下有类别时不可删）
- **类别**：可新增任意类别（如 📚 学习计划），指定所属分组，设置专属 Emoji
- **子项**：每个类别下可自由增删子项（如「读书 / 课程 / 复盘」），每个子项都可配独立 Emoji
- **Emoji 面板**：编辑时点击面板中的表情即可填入，也支持直接粘贴任意 Emoji
- **改名安全**：修改类别或子项名称时，已有的计划、模板、默认值会自动同步更新
- 所有改动即时保存并同步到所有设备

---

## 七、多设备同步部署

### 方式 A：NAS WebDAV（推荐，零程序）

适用于群晖、威联通、绿联、极空间等主流 NAS：

1. 在 NAS 管理界面开启 **WebDAV 服务**，记下端口号
2. 新建一个共享文件夹（如 `dayplan`），把 `index.html` 上传进去
3. 手机 / 平板 / 电脑浏览器访问：
   `http://NAS的IP:端口/dayplan/index.html`（输入 NAS 账号密码）
4. 首次保存时自动创建 `dayplan.json`，此后**所有设备读写同一份数据，实时一致**

> 配合 NAS 自带的远程访问功能（各品牌均有），出门在外也能使用同一份数据。
> 建议将页面地址添加到手机主屏幕，体验接近原生 App。

### 方式 B：sync_server.py（带版本快照回滚）

```
python sync_server.py
```

- 浏览器访问 `http://电脑或NAS的IP:8000/index.html`
- 每次保存自动在 `snapshots/` 目录留快照（保留最近 50 份），误删可从 设置 → 版本快照 恢复
- NAS Docker 部署：镜像 `python:3-alpine`，挂载本目录到 `/app`，命令 `python /app/sync_server.py`，映射端口 8000

### 本地模式与服务器模式互通

- `file://` 双击打开 = 本地模式（localStorage）；`http://` 访问 = 服务器模式，自动切换
- 服务器模式打开时会**自动合并**：服务器没数据则推送本地数据；两边都有则按修改时间合并，不互相覆盖
- 本地模式可在 设置 → 远程同步 填入服务器地址，手动「拉取合并」/「推送上传」
- 手机离线时的改动先存本地，恢复连接后自动补传

---

## 八、导入与导出

| 格式 | 内容 | 适用 |
|---|---|---|
| CSV | 日期, 时间, 类别, 子类别, 标题, 优先级, 状态, 链接, 备注 | Excel 查看 / 编辑 / 批量导入 |
| JSON | 完整备份（含图片、模板、类别配置、默认值） | 跨设备完整迁移 / 定期备份 |

- 导出可指定时间范围：本周 / 本月 / 今年 / 全部 / 自定义起止，导出前显示条数
- `导入模板.csv` 是可直接填写的批量导入模板（含示例行）
- 注意：图片体积较大，CSV 不包含图片；完整迁移请用 JSON

---

## 九、家庭多人使用（多用户）

适合一家人或小团队共用同一个 NAS 部署：每人一份完全独立的数据（计划、模板、类别配置、默认值），互不干扰、永不冲突。

### 开启方式

1. 点顶栏的用户徽章（👤 默认 ▾）→ 用户面板
2. 「＋ 创建用户」：输入名字（可配一个专属 Emoji），可勾选**复制现有用户的类别/模板/默认值配置**，省去重配
3. 创建后自动切换到该用户，数据保存在 NAS 的 users/名字.json
4. 不创建任何用户时，一切照旧使用默认的 dayplan.json

### 切换与直达

- 随时点用户徽章切换，切换即时生效（数据都是实时保存的）
- **推荐**：手机主屏幕为每人添加一个直达图标——
  妈妈：`http://NAS地址:端口/?u=妈妈`　爸爸：`?u=爸爸`
  点开就是自己的计划，无需任何选择步骤
- 页面会记住每台设备上次使用的用户

### 备份与删除

- 导出的 CSV/JSON 文件名自动带用户名；JSON 备份内嵌用户名，导入时若属于其他用户会自动询问切换
- 删除用户前会**强制先下载完整备份**，再从服务器删除文件（sync_server 模式下每个用户的保存也都有独立快照）

### 需要知道的边界

- 用户隔离是「数据分开」，不是「隐私权限」——知道 NAS 地址的人仍可切换查看任何用户（如需真隐私，请在 NAS 上为每人建立独立 WebDAV 账号映射不同目录）
- 用户名不可包含  / : * ? 等文件名非法字符

---

## 十、键盘快捷键（电脑端）

| 按键 | 功能 |
|---|---|
| `N` | 新建计划（打开类别名片） |
| `1` / `2` / `3` | 切换 日 / 周 / 总览 视图 |
| `←` / `→` | 上 / 下一天（日视图）或上 / 下周（周视图） |
| `F` | 循环切换筛选：全部 → 未完成 → 已完成 → 逾期 |
| `E` | 打开导出面板 |
| `回车` | 表单中快速保存（标题输入框内） |

---

## 十一、常见问题

**Q：数据会传到云端吗？**
不会。所有数据都存在你的浏览器或你自己的 NAS / 电脑上，没有任何第三方服务参与。

**Q：换浏览器 / 换电脑后数据不见了？**
本地模式的数据只存在当前浏览器。换设备前请先导出 JSON，或尽早切换到 NAS / sync_server 同步模式。

**Q：图片为什么建议少放？**
浏览器本地存储约 5MB 上限，行程图片已自动压缩，但仍建议只放必要图片。页面底部会显示当前占用。

**Q：两个人同时编辑会冲突吗？**
同步按「最近修改优先」自动合并，不同任务互不影响；同一任务同时修改时以较新版本为准。重要数据建议定期导出 JSON 备份。

**Q：支持哪些浏览器？**
Chrome、Edge、Safari、Firefox 近两年版本均可；手机浏览器直接打开文件或网址即可。

---

*今日毕 · DoneToday — 让每一天清晰可见。*
